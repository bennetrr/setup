name: Testing

on:
  pull_request:
    branches: [ main ]

  workflow_dispatch:
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Test the script
        run: |
          chmod a+x setup.sh
          ./setup.sh --graphical